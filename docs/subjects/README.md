# Subject Docs

This folder holds subject-specific documentation that should not live under `output/`.

Rule:

- generated scripts, episode plans, dossiers, and media stay under `output/`
- stable subject documentation such as curriculum maps, inventories, and production notes live here

Current subjects:

- `physics/`

Inside `physics/`, future sessions should use:

- `curriculum.md` for module structure
- `asset-inventory.md` for reusable visual needs
- `scene-design/` for production-ready pilot scene boards
