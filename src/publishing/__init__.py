"""
Publishing integrations for external platforms.

This package currently provides a shared YouTube integration layer that:

- stores reusable OAuth credentials in one local config directory
- uploads videos and thumbnails via the YouTube Data API
- fetches channel/video analytics via the YouTube Analytics API
- launches YouTube Studio in a shared Chrome profile for browser-only actions
"""

__all__: list[str] = []
